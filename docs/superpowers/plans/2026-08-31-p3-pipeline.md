# P3 流水线（pipeline + CLI）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 实现 `pipeline.py`——把 P2 的 feeds / extract / summarize / builders / xteink 按 spec 第 6 节步骤 0→7 编排成「执行一个 task」的单一入口——外加一个 CLI（`python -m push2xteink run <task_id>` / `list`）。

**Architecture:** 一个 `Pipeline` 类，持有 `Config` + `State` + 长期的 `Summarizer` / `XteinkClient`（spec 第 14 节：跨 run 复用，reload 时 `close`）。`run_task(task_id)` 跑完整流水线，**任何情况下都写一行 `runs` 记录并返回 `RunOutcome`，绝不向上抛异常**（P4 调度器依赖这一点）。协作者（模块级函数 `fetch_feed` / `select_new_articles` / `apply_full_text` / `build_*` / `html_to_text`）直接 import 调用，测试用 monkeypatch；`Summarizer` / `XteinkClient` 通过构造函数注入，测试传 fake。CLI 是薄壳：解析配置路径、构造 `Pipeline`、跑一个 task、按结果设退出码。

**Tech Stack:** Python 3.12、标准库（`argparse`、`concurrent.futures`、`tempfile`、`shutil`）、pytest。无新第三方依赖。

**Spec:** `docs/superpowers/specs/2026-08-31-push2xteink-design.md` 第 6 节（流水线步骤 0→7 + 去重一致性）、第 10 节（`CONFIG_PATH` / `DB_PATH` 环境变量）、第 13、14 节（P1/P2 遗留项与契约）。

## Global Constraints

- Python `>=3.12`；`X | None` / `list[...]` / `dict[...]` 标注。
- `run_task` **绝不向调用方抛异常**——`BuildError` / `XteinkUploadError` / 任何 `Exception` 都要捕获，写 `runs` 行 `status='failed'`，返回 `RunOutcome(status="failed", ...)`。
- 所有 datetime 是 tz-aware UTC；`run_task` 的 `now` 参数为 `None` 时取 `datetime.now(timezone.utc)`，naive 补 UTC。
- **上传失败绝不 `mark_pushed`**（spec 第 6 节步骤 6：条目留待窗口内重试）。只有 `push_file` 返回后才对本次 `kept_guids` 调 `state.mark_pushed`。
- 代理：调 `fetch_feed` / `apply_full_text` 时，`proxy_url = config.proxy.url if feed.use_proxy else None`。调 `Summarizer` 构造时传 `proxy_url=config.proxy.url`（`Summarizer` 内部按 `config.ai.use_proxy` 再 gate）。`XteinkClient` 不传代理。
- 摘要输入用 `push2xteink.builders.common.html_to_text(article.content_html)`，不要把原始 HTML 给 LLM（spec 第 14 节）。
- 源文件 `src/push2xteink/`，测试 `tests/`。

## P1/P2 依赖签名（已合并在 master，逐字对照，不可修改这些模块）

```python
# feeds
def fetch_feed(feed: Feed, *, proxy_url: str | None = None, timeout: float = 20.0) -> FeedResult
    # FeedResult(articles: list[Article], error: str | None)
def select_new_articles(state: State, feed_id: str, articles: list[Article], *,
                        first_run: bool, lookback_hours: int, now: datetime | None = None) -> list[Article]
    # 已对保留项调用 state.record_seen
# extract
def apply_full_text(article: Article, *, enabled: bool, proxy_url: str | None = None,
                    timeout: float = 20.0) -> Article
# summarize
class Summarizer:
    def __init__(self, config: AIConfig, *, proxy_url: str | None = None) -> None
    def summarize(self, text: str) -> str          # 失败抛 SummarizeError
    def close(self) -> None ; __enter__ ; __exit__
class SummarizeError(Exception): ...
# builders
def build_epub(title: str, articles: list[Article], *, out_dir: Path) -> Path   # <256B -> BuildError
def build_txt(title: str, articles: list[Article], *, out_dir: Path) -> Path
class BuildError(Exception): ...
def html_to_text(html: str) -> str
# xteink
class XteinkClient:
    def __init__(self, config: XteinkConfig, state: State, *, timeout: float = 30.0) -> None
    def push_file(self, path: Path, filename: str) -> str    # 失败抛 XteinkUploadError
    def close(self) -> None ; __enter__ ; __exit__
class XteinkUploadError(Exception): ...
# state
class State:
    def start_run(self, task_id: str, *, now: datetime | None = None) -> int
    def finish_run(self, run_id: int, *, status: str, item_count: int | None = None,
                   file_name: str | None = None, message: str | None = None,
                   now: datetime | None = None) -> None
    def task_has_successful_run(self, task_id: str) -> bool
    def recent_runs(self, limit: int = 50) -> list      # sqlite3.Row rows, keys: task_id,status,started_at,...
    def mark_pushed(self, feed_id: str, guids: list[str], *, now: datetime | None = None) -> None
# config / models
def load_config(path: Path) -> Config ; class ConfigError(Exception)
# Config: .feeds list[Feed], .tasks list[Task], .proxy.url str|None,
#         .fetch.timeout_seconds int, .fetch.concurrency int, .ai AIConfig|None, .xteink XteinkConfig
# Task: .id .name .feeds(list[str]) .schedule .summarize .format("epub"|"txt") .enabled .first_run_lookback_hours
# Feed: .id .url .full_text .use_proxy
```

## File Structure

| 文件 | 职责 |
|---|---|
| `src/push2xteink/pipeline.py` | `RunOutcome`、`Pipeline`（`from_config` / `run_task` / `close` / ctx mgr + 私有步骤方法） |
| `src/push2xteink/cli.py` | `main(argv: list[str] | None = None) -> int`（`run` / `list` 子命令） |
| `src/push2xteink/__main__.py` | `import sys; from .cli import main; sys.exit(main())` |
| `tests/test_pipeline.py` | 流水线单测（协作者全 fake/monkeypatch） |
| `tests/test_cli.py` | CLI 单测 |
| `tests/conftest.py` | 追加 `pipeline_config` fixture（一个可用的 `Config` 对象） |

## Interfaces（本计划对外产出）

```python
# push2xteink.pipeline
@dataclasses.dataclass
class RunOutcome:
    task_id: str
    status: str                    # "success" | "skipped" | "failed"
    item_count: int = 0
    file_name: str | None = None
    record_id: str | None = None
    message: str | None = None     # 告警/错误，多行；无则 None

class Pipeline:
    def __init__(self, config: Config, state: State, *,
                 summarizer: "Summarizer | None", xteink_client: "XteinkClient",
                 output_dir: str | pathlib.Path | None = None) -> None
    @classmethod
    def from_config(cls, config: Config, state: State, *,
                    output_dir: str | pathlib.Path | None = None) -> "Pipeline"
    def run_task(self, task_id: str, *, now: datetime | None = None) -> RunOutcome
    def close(self) -> None
    def __enter__(self) -> "Pipeline" ; def __exit__(self, *exc) -> None

# push2xteink.cli
def main(argv: list[str] | None = None) -> int      # 0 = success/skipped, 1 = failed run, 2 = config/usage error
```

---

## Task 1: `RunOutcome` + `Pipeline` 骨架（构造 / from_config / close / ctx mgr）

**Files:**
- Create: `src/push2xteink/pipeline.py`
- Create: `tests/test_pipeline.py`
- Modify: `tests/conftest.py`

**Interfaces:**
- Consumes: P1/P2 全部（见上）
- Produces: `RunOutcome`, `Pipeline.__init__`, `Pipeline.from_config`, `Pipeline.close`, `__enter__`/`__exit__`

- [ ] **Step 1: conftest 追加 fixture**

在 `tests/conftest.py` 追加：

```python
from pathlib import Path

from push2xteink.models import (
    AIConfig, AIProvider, Config, Feed, FetchConfig, ProxyConfig, Task, XteinkConfig,
)


@pytest.fixture
def pipeline_config() -> Config:
    return Config(
        xteink=XteinkConfig(username="u", password="p"),
        proxy=ProxyConfig(url=None),
        fetch=FetchConfig(timeout_seconds=5, concurrency=3),
        ai=AIConfig(primary=AIProvider(base_url="https://ai/v1", api_key="k", model="m")),
        feeds=[
            Feed(id="a", url="https://a.example/rss", full_text=True),
            Feed(id="b", url="https://b.example/rss", full_text=False),
        ],
        tasks=[
            Task(id="brief", name="早报", feeds=["a", "b"], schedule="0 7 * * *",
                 summarize=True, format="epub", first_run_lookback_hours=48),
            Task(id="plain", name="纯文", feeds=["a"], schedule="0 8 * * *",
                 summarize=False, format="txt"),
        ],
    )
```

- [ ] **Step 2: 写失败测试**

`tests/test_pipeline.py`：

```python
from pathlib import Path

from push2xteink.pipeline import Pipeline, RunOutcome
from push2xteink.state import State


class FakeSummarizer:
    def __init__(self): self.closed = False
    def summarize(self, text): return f"summary of {text[:10]}"
    def close(self): self.closed = True


class FakeXteink:
    def __init__(self): self.closed = False; self.pushed = []
    def push_file(self, path, filename): self.pushed.append((Path(path).name, filename)); return "rec-1"
    def close(self): self.closed = True


def test_run_outcome_defaults():
    o = RunOutcome(task_id="x", status="skipped")
    assert o.item_count == 0 and o.file_name is None and o.message is None


def test_pipeline_close_closes_collaborators(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    fs, fx = FakeSummarizer(), FakeXteink()
    p = Pipeline(pipeline_config, s, summarizer=fs, xteink_client=fx)
    p.close()
    assert fs.closed and fx.closed
    s.close()


def test_pipeline_context_manager(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    fx = FakeXteink()
    with Pipeline(pipeline_config, s, summarizer=None, xteink_client=fx) as p:
        assert isinstance(p, Pipeline)
    assert fx.closed
    s.close()


def test_from_config_builds_real_collaborators(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    p = Pipeline.from_config(pipeline_config, s)
    from push2xteink.summarize import Summarizer
    from push2xteink.xteink import XteinkClient
    assert isinstance(p._summarizer, Summarizer)   # ai configured
    assert isinstance(p._xteink, XteinkClient)
    p.close()
    s.close()


def test_from_config_no_summarizer_when_ai_absent(pipeline_config, tmp_path):
    cfg = pipeline_config.model_copy(update={"ai": None})
    cfg = cfg.model_copy(update={"tasks": [t.model_copy(update={"summarize": False}) for t in cfg.tasks]})
    s = State(tmp_path / "s.db")
    p = Pipeline.from_config(cfg, s)
    assert p._summarizer is None
    p.close()
    s.close()
```

- [ ] **Step 3: 运行 → 失败**（`ModuleNotFoundError: push2xteink.pipeline`）

- [ ] **Step 4: 实现**

```python
from __future__ import annotations

import dataclasses
from pathlib import Path

from .config import Config
from .models import Config as _Config  # noqa: F401  (type only; Config re-exported from config? see note)
from .state import State
from .summarize import Summarizer
from .xteink import XteinkClient


@dataclasses.dataclass
class RunOutcome:
    task_id: str
    status: str
    item_count: int = 0
    file_name: str | None = None
    record_id: str | None = None
    message: str | None = None


class Pipeline:
    def __init__(
        self,
        config: Config,
        state: State,
        *,
        summarizer: Summarizer | None,
        xteink_client: XteinkClient,
        output_dir: str | Path | None = None,
    ) -> None:
        self._config = config
        self._state = state
        self._summarizer = summarizer
        self._xteink = xteink_client
        self._output_dir = Path(output_dir) if output_dir is not None else None

    @classmethod
    def from_config(
        cls, config: Config, state: State, *, output_dir: str | Path | None = None
    ) -> "Pipeline":
        summarizer = (
            Summarizer(config.ai, proxy_url=config.proxy.url) if config.ai is not None else None
        )
        xteink = XteinkClient(config.xteink, state)
        return cls(config, state, summarizer=summarizer, xteink_client=xteink,
                   output_dir=output_dir)

    def close(self) -> None:
        if self._summarizer is not None:
            self._summarizer.close()
        self._xteink.close()

    def __enter__(self) -> "Pipeline":
        return self

    def __exit__(self, *exc: object) -> None:
        self.close()

    def run_task(self, task_id: str, *, now=None) -> RunOutcome:  # Task 5
        raise NotImplementedError
```

> **Note on `Config` import**: P1 defined models in `push2xteink.models` and `push2xteink.config` re-exports/uses it. Import `Config` from wherever P1 actually exports it — check `src/push2xteink/config.py` and `models.py`. Use `from .models import Config` if that's the home; drop the confusing double-import in the snippet above and just `from .models import Config`.

- [ ] **Step 5: 运行 → 全绿。Commit** `feat: Pipeline skeleton + RunOutcome`

---

## Task 2: 步骤 1–2 —— `_gather`（拉源 + 去重）

**Files:**
- Modify: `src/push2xteink/pipeline.py`
- Modify: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: `feeds.fetch_feed`, `feeds.select_new_articles`, `State.task_has_successful_run`
- Produces: `Pipeline._gather(task, *, now, warnings: list[str]) -> tuple[list[Article], dict[str, list[str]]]`
  - 返回 `(保留的 Article 列表, {feed_id: [guid,...]})`
  - 单源 `FeedResult.error` → 追加到 `warnings`，跳过该源，继续其它源（**不抛**）
  - `first_run = not state.task_has_successful_run(task.id)`

- [ ] **Step 1: 写失败测试**

```python
from datetime import datetime, timezone

from push2xteink.feeds import FeedResult
from push2xteink.models import Article

NOW = datetime(2026, 8, 31, 12, 0, tzinfo=timezone.utc)


def _art(feed_id, guid):
    return Article(feed_id=feed_id, guid=guid, title="t", link=f"https://x/{guid}",
                   content_html="<p>c</p>")


def _pipe(cfg, state):
    return Pipeline(cfg, state, summarizer=FakeSummarizer(), xteink_client=FakeXteink())


def test_gather_collects_new_from_all_feeds(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    def fake_fetch(feed, **kw):
        return FeedResult(articles=[_art(feed.id, f"{feed.id}1"), _art(feed.id, f"{feed.id}2")])
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed", fake_fetch)

    task = pipeline_config.tasks[0]
    warnings = []
    arts, guids = _pipe(pipeline_config, s)._gather(task, now=NOW, warnings=warnings)
    assert {a.guid for a in arts} == {"a1", "a2", "b1", "b2"}
    assert guids == {"a": ["a1", "a2"], "b": ["b1", "b2"]}
    assert warnings == []
    s.close()


def test_gather_feed_error_is_warned_not_raised(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    def fake_fetch(feed, **kw):
        if feed.id == "a":
            return FeedResult(error="boom")
        return FeedResult(articles=[_art("b", "b1")])
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed", fake_fetch)
    warnings = []
    arts, guids = _pipe(pipeline_config, s)._gather(pipeline_config.tasks[0], now=NOW, warnings=warnings)
    assert [a.guid for a in arts] == ["b1"]
    assert any("a" in w and "boom" in w for w in warnings)
    s.close()


def test_gather_dedups_across_runs(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed",
                        lambda feed, **kw: FeedResult(articles=[_art(feed.id, f"{feed.id}1")]))
    p = _pipe(pipeline_config, s)
    task = pipeline_config.tasks[0]
    first, _ = p._gather(task, now=NOW, warnings=[])
    assert len(first) == 2
    # mark them pushed so they're not pushable again
    s.mark_pushed("a", ["a1"], now=NOW); s.mark_pushed("b", ["b1"], now=NOW)
    second, _ = p._gather(task, now=NOW, warnings=[])
    assert second == []
    s.close()
```

- [ ] **Step 2: 运行 → 失败**

- [ ] **Step 3: 实现** —— 在 `pipeline.py` 顶部加 import，`Pipeline` 加方法：

```python
from datetime import datetime, timezone

from .feeds import fetch_feed, select_new_articles
from .models import Article
```

```python
    def _feed(self, feed_id: str) -> "Feed":
        for f in self._config.feeds:
            if f.id == feed_id:
                return f
        raise KeyError(f"unknown feed {feed_id!r}")

    def _task(self, task_id: str):
        for t in self._config.tasks:
            if t.id == task_id:
                return t
        raise KeyError(f"unknown task {task_id!r}")

    def _proxy_for(self, feed) -> str | None:
        return self._config.proxy.url if feed.use_proxy else None

    def _gather(self, task, *, now: datetime, warnings: list[str]):
        first_run = not self._state.task_has_successful_run(task.id)
        kept: list[Article] = []
        kept_guids: dict[str, list[str]] = {}
        for feed_id in task.feeds:
            feed = self._feed(feed_id)
            result = fetch_feed(
                feed, proxy_url=self._proxy_for(feed),
                timeout=self._config.fetch.timeout_seconds,
            )
            if result.error:
                warnings.append(f"feed {feed_id}: {result.error}")
                continue
            new = select_new_articles(
                self._state, feed_id, result.articles,
                first_run=first_run,
                lookback_hours=task.first_run_lookback_hours,
                now=now,
            )
            if new:
                kept.extend(new)
                kept_guids[feed_id] = [a.guid for a in new]
        return kept, kept_guids
```

（`Feed` 类型标注：`from .models import Feed`，或去掉字符串标注。）

- [ ] **Step 4: 运行 → 全绿。Commit** `feat: pipeline _gather (fetch + dedup)`

---

## Task 3: 步骤 3–4 —— `_prepare`（并发取正文 + 顺序摘要）

**Files:**
- Modify: `src/push2xteink/pipeline.py`
- Modify: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: `extract.apply_full_text`, `builders.common.html_to_text`, `Summarizer.summarize`, `SummarizeError`
- Produces: `Pipeline._prepare(task, articles: list[Article], *, warnings: list[str]) -> list[Article]`
  - 步骤 3：`ThreadPoolExecutor(max_workers=config.fetch.concurrency)` 并发对每篇跑 `apply_full_text(art, enabled=feed.full_text, proxy_url=..., timeout=...)`；保持输入顺序。
  - 步骤 4：仅当 `task.summarize` —— 顺序对每篇 `art.summary = self._summarizer.summarize(html_to_text(art.content_html))`；`SummarizeError` → 追加 warning，该篇 `summary` 留 `None`，继续。
  - 返回处理后的 articles（顺序不变）。

- [ ] **Step 1: 写失败测试**

```python
def test_prepare_applies_full_text_concurrently_preserving_order(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    seen = []
    def fake_aft(article, *, enabled, **kw):
        seen.append((article.guid, enabled))
        return article.model_copy(update={"content_html": f"<p>full {article.guid}</p>",
                                          "content_is_full_text": enabled})
    monkeypatch.setattr("push2xteink.pipeline.apply_full_text", fake_aft)
    arts = [_art("a", "a1"), _art("a", "a2"), _art("b", "b1")]
    out = _pipe(pipeline_config, s)._prepare(pipeline_config.tasks[0], arts, warnings=[])
    assert [a.guid for a in out] == ["a1", "a2", "b1"]           # order preserved
    assert out[0].content_html == "<p>full a1</p>"
    assert dict(seen)["a1"] is True and dict(seen)["b1"] is False  # feed.full_text honored
    s.close()


def test_prepare_summarizes_when_task_wants_it(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.apply_full_text", lambda a, **kw: a)
    fs = FakeSummarizer()
    p = Pipeline(pipeline_config, s, summarizer=fs, xteink_client=FakeXteink())
    out = p._prepare(pipeline_config.tasks[0], [_art("a", "a1")], warnings=[])   # tasks[0].summarize=True
    assert out[0].summary and out[0].summary.startswith("summary of")
    s.close()


def test_prepare_no_summary_when_task_opts_out(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.apply_full_text", lambda a, **kw: a)
    p = Pipeline(pipeline_config, s, summarizer=FakeSummarizer(), xteink_client=FakeXteink())
    out = p._prepare(pipeline_config.tasks[1], [_art("a", "a1")], warnings=[])   # tasks[1].summarize=False
    assert out[0].summary is None
    s.close()


def test_prepare_summary_failure_is_warned_and_skipped(pipeline_config, tmp_path, monkeypatch):
    from push2xteink.summarize import SummarizeError
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.apply_full_text", lambda a, **kw: a)
    class BadSummarizer:
        def summarize(self, text): raise SummarizeError("nope")
        def close(self): pass
    p = Pipeline(pipeline_config, s, summarizer=BadSummarizer(), xteink_client=FakeXteink())
    warnings = []
    out = p._prepare(pipeline_config.tasks[0], [_art("a", "a1"), _art("a", "a2")], warnings=warnings)
    assert all(a.summary is None for a in out)
    assert len(warnings) == 2 and all("summary failed" in w for w in warnings)
    s.close()
```

- [ ] **Step 2: 运行 → 失败**

- [ ] **Step 3: 实现** —— 加 import + 方法：

```python
from concurrent.futures import ThreadPoolExecutor

from .builders import html_to_text
from .extract import apply_full_text
from .summarize import SummarizeError
```

```python
    def _prepare(self, task, articles: list[Article], *, warnings: list[str]) -> list[Article]:
        def do_extract(article: Article) -> Article:
            feed = self._feed(article.feed_id)
            return apply_full_text(
                article, enabled=feed.full_text,
                proxy_url=self._proxy_for(feed),
                timeout=self._config.fetch.timeout_seconds,
            )

        workers = max(1, self._config.fetch.concurrency)
        with ThreadPoolExecutor(max_workers=workers) as pool:
            articles = list(pool.map(do_extract, articles))

        if task.summarize and self._summarizer is not None:
            for article in articles:
                try:
                    article.summary = self._summarizer.summarize(
                        html_to_text(article.content_html)
                    )
                except SummarizeError as exc:
                    warnings.append(f"summary failed for {article.link}: {exc}")
        return articles
```

> `article.summary = ...` 直接改 pydantic 模型字段——P1 的 `Article` 未设 `frozen`，可写。`html_to_text` 从 `push2xteink.builders` re-export（P2 已导出）。

- [ ] **Step 4: 运行 → 全绿。Commit** `feat: pipeline _prepare (full-text + summary)`

---

## Task 4: 步骤 5 —— `_build`（标题 / 同日防撞 / 生成文件）

**Files:**
- Modify: `src/push2xteink/pipeline.py`
- Modify: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: `builders.build_epub`, `builders.build_txt`, `State.recent_runs`
- Produces: `Pipeline._build(task, articles, *, now: datetime, out_dir: Path) -> Path`
  - `title = f"{task.name}_{now:%Y%m%d}"`；若 `state.recent_runs` 里有本 task 今天（`started_at` 以 `now` 的 `%Y-%m-%d` 开头）且 `status=='success'` 的记录 → `title = f"{title}_{now:%H%M%S}"`。
  - `task.format == "epub"` → `build_epub(title, articles, out_dir=out_dir)`；否则 `build_txt(...)`。
  - `BuildError` 原样向上抛（由 `run_task` 捕获）。

- [ ] **Step 1: 写失败测试**

```python
from push2xteink.builders.common import BuildError


def test_build_epub_default_title(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    arts = [_art("a", "a1")]
    # give the article real body so EPUB clears the 256B floor
    arts[0].content_html = "<p>" + "长正文内容。" * 50 + "</p>"
    p = _pipe(pipeline_config, s)
    path = p._build(pipeline_config.tasks[0], arts, now=NOW, out_dir=tmp_path)
    assert path.name == "早报_20260831.epub"
    s.close()


def test_build_txt_for_txt_task(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    p = _pipe(pipeline_config, s)
    path = p._build(pipeline_config.tasks[1], [_art("a", "a1")], now=NOW, out_dir=tmp_path)
    assert path.name == "纯文_20260831.txt" and path.read_text(encoding="utf-8")
    s.close()


def test_build_appends_time_on_same_day_rerun(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    rid = s.start_run("plain", now=NOW)
    s.finish_run(rid, status="success", now=NOW)
    p = _pipe(pipeline_config, s)
    later = NOW.replace(hour=15, minute=30, second=45)
    path = p._build(pipeline_config.tasks[1], [_art("a", "a1")], now=later, out_dir=tmp_path)
    assert path.name == "纯文_20260831_153045.txt"
    s.close()


def test_build_error_propagates(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.build_epub",
                        lambda *a, **k: (_ for _ in ()).throw(BuildError("too small")))
    p = _pipe(pipeline_config, s)
    import pytest
    with pytest.raises(BuildError):
        p._build(pipeline_config.tasks[0], [_art("a", "a1")], now=NOW, out_dir=tmp_path)
    s.close()
```

- [ ] **Step 2: 运行 → 失败**

- [ ] **Step 3: 实现** —— 加 import + 方法：

```python
from .builders import build_epub, build_txt
```

```python
    def _same_day_success(self, task_id: str, now: datetime) -> bool:
        day = now.strftime("%Y-%m-%d")
        for row in self._state.recent_runs(200):
            if (row["task_id"] == task_id and row["status"] == "success"
                    and str(row["started_at"]).startswith(day)):
                return True
        return False

    def _build(self, task, articles: list[Article], *, now: datetime, out_dir: Path) -> Path:
        title = f"{task.name}_{now:%Y%m%d}"
        if self._same_day_success(task.id, now):
            title = f"{title}_{now:%H%M%S}"
        if task.format == "epub":
            return build_epub(title, articles, out_dir=out_dir)
        return build_txt(title, articles, out_dir=out_dir)
```

- [ ] **Step 4: 运行 → 全绿。Commit** `feat: pipeline _build (title + same-day collision + builder dispatch)`

---

## Task 5: `run_task` —— 编排 0→7 + 失败隔离

**Files:**
- Modify: `src/push2xteink/pipeline.py`
- Modify: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: 前面所有私有方法、`State.start_run` / `finish_run` / `mark_pushed`、`BuildError`、`XteinkUploadError`
- Produces: `Pipeline.run_task(task_id: str, *, now: datetime | None = None) -> RunOutcome`

**流程**（spec 第 6 节 0→7）：
```
now 规范化（None -> utcnow；naive -> +UTC）
task = self._task(task_id)                     # KeyError -> 见下
run_id = state.start_run(task_id, now=now)
warnings = []
临时目录：output_dir 有则用它（不删）；否则 tempfile.mkdtemp()（finally 删）
try:
    articles, kept_guids = _gather(task, now, warnings)
    if not articles:
        state.finish_run(run_id, status="skipped", item_count=0, message=_join(warnings, "no new items"), now=now)
        return RunOutcome(task_id, "skipped", 0, message=_join(warnings, "no new items"))
    articles = _prepare(task, articles, warnings=warnings)
    path = _build(task, articles, now=now, out_dir=tmpdir)
    record_id = self._xteink.push_file(path, path.name)     # 只有到这一步成功才 mark_pushed
    for feed_id, guids in kept_guids.items():
        state.mark_pushed(feed_id, guids, now=now)
    msg = _join(warnings, None)
    state.finish_run(run_id, status="success", item_count=len(articles), file_name=path.name, message=msg, now=now)
    return RunOutcome(task_id, "success", len(articles), file_name=path.name, record_id=record_id, message=msg)
except (BuildError, XteinkUploadError) as exc:
    msg = _join(warnings, f"{type(exc).__name__}: {exc}")
    state.finish_run(run_id, status="failed", message=msg, now=now)
    return RunOutcome(task_id, "failed", message=msg)
except Exception as exc:                                    # 兜底：绝不让 run 崩掉调度器
    msg = _join(warnings, f"unexpected error: {exc!r}")
    state.finish_run(run_id, status="failed", message=msg, now=now)
    return RunOutcome(task_id, "failed", message=msg)
finally:
    如果是自建 tmpdir: shutil.rmtree(tmpdir, ignore_errors=True)
```
`_task(task_id)` 抛 `KeyError` 时：**在 `start_run` 之前**发生，所以还没有 run 行——`run_task` 直接 `return RunOutcome(task_id, "failed", message=f"unknown task {task_id!r}")`（不写 runs）。把 `_task` 调用放在 `start_run` 之前并单独 try。

`_join(warnings, extra)`：把 `warnings + ([extra] if extra else [])` 里非空项用 `"\n"` 连接；全空 → `None`。

- [ ] **Step 1: 写失败测试**

```python
import pytest

from push2xteink.builders.common import BuildError
from push2xteink.xteink import XteinkUploadError


def _fetch_two(feed, **kw):
    return FeedResult(articles=[_art(feed.id, f"{feed.id}1")])


def _full_pipe(cfg, s, monkeypatch, *, xteink=None):
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed", _fetch_two)
    monkeypatch.setattr("push2xteink.pipeline.apply_full_text",
                        lambda a, **kw: a.model_copy(update={"content_html": "<p>" + "x" * 400 + "</p>"}))
    return Pipeline(cfg, s, summarizer=FakeSummarizer(), xteink_client=xteink or FakeXteink())


def test_run_task_success_marks_pushed_and_writes_run(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    fx = FakeXteink()
    p = _full_pipe(pipeline_config, s, monkeypatch, xteink=fx)
    out = p.run_task("brief", now=NOW)
    assert out.status == "success" and out.item_count == 2 and out.record_id == "rec-1"
    assert out.file_name == "早报_20260831.epub"
    assert fx.pushed == [("早报_20260831.epub", "早报_20260831.epub")]
    assert s.is_item_pushable("a", "a1", 48, now=NOW) is False  # marked pushed
    assert s.task_has_successful_run("brief") is True
    row = s.recent_runs(1)[0]
    assert row["status"] == "success" and row["item_count"] == 2
    s.close()


def test_run_task_no_new_items_is_skipped(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed", lambda feed, **kw: FeedResult(articles=[]))
    p = Pipeline(pipeline_config, s, summarizer=FakeSummarizer(), xteink_client=FakeXteink())
    out = p.run_task("brief", now=NOW)
    assert out.status == "skipped" and out.item_count == 0
    assert s.recent_runs(1)[0]["status"] == "skipped"
    s.close()


def test_run_task_upload_failure_does_not_mark_pushed(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    class FailXteink:
        def push_file(self, p, f): raise XteinkUploadError("500")
        def close(self): pass
    p = _full_pipe(pipeline_config, s, monkeypatch, xteink=FailXteink())
    out = p.run_task("brief", now=NOW)
    assert out.status == "failed" and "XteinkUploadError" in out.message
    assert s.is_item_pushable("a", "a1", 48, now=NOW) is True   # NOT marked -> retried later
    assert s.recent_runs(1)[0]["status"] == "failed"
    s.close()


def test_run_task_build_failure_is_failed(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    p = _full_pipe(pipeline_config, s, monkeypatch)
    monkeypatch.setattr("push2xteink.pipeline.build_epub",
                        lambda *a, **k: (_ for _ in ()).throw(BuildError("too small")))
    out = p.run_task("brief", now=NOW)
    assert out.status == "failed" and "BuildError" in out.message
    s.close()


def test_run_task_unexpected_error_is_caught(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed",
                        lambda *a, **k: (_ for _ in ()).throw(RuntimeError("boom")))
    p = Pipeline(pipeline_config, s, summarizer=FakeSummarizer(), xteink_client=FakeXteink())
    out = p.run_task("brief", now=NOW)
    assert out.status == "failed" and "unexpected" in out.message
    assert s.recent_runs(1)[0]["status"] == "failed"
    s.close()


def test_run_task_unknown_task_no_run_row(pipeline_config, tmp_path):
    s = State(tmp_path / "s.db")
    p = Pipeline(pipeline_config, s, summarizer=None, xteink_client=FakeXteink())
    out = p.run_task("ghost", now=NOW)
    assert out.status == "failed" and "unknown task" in out.message
    assert s.recent_runs(10) == []
    s.close()


def test_run_task_feed_warning_recorded_on_success(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    def fetch(feed, **kw):
        return FeedResult(error="down") if feed.id == "b" else FeedResult(articles=[_art("a", "a1")])
    monkeypatch.setattr("push2xteink.pipeline.fetch_feed", fetch)
    monkeypatch.setattr("push2xteink.pipeline.apply_full_text",
                        lambda a, **kw: a.model_copy(update={"content_html": "<p>" + "x" * 400 + "</p>"}))
    p = Pipeline(pipeline_config, s, summarizer=FakeSummarizer(), xteink_client=FakeXteink())
    out = p.run_task("brief", now=NOW)
    assert out.status == "success" and out.item_count == 1
    assert "feed b" in (s.recent_runs(1)[0]["message"] or "")
    s.close()


def test_run_task_naive_now_accepted(pipeline_config, tmp_path, monkeypatch):
    s = State(tmp_path / "s.db")
    p = _full_pipe(pipeline_config, s, monkeypatch)
    out = p.run_task("brief", now=datetime(2026, 8, 31, 12, 0))  # naive
    assert out.status == "success"
    s.close()
```

- [ ] **Step 2: 运行 → 失败**

- [ ] **Step 3: 实现** —— 加 import + 替换 `run_task` 桩：

```python
import shutil
import tempfile
```

```python
    @staticmethod
    def _join(warnings: list[str], extra: str | None) -> str | None:
        parts = [w for w in warnings if w]
        if extra:
            parts.append(extra)
        return "\n".join(parts) if parts else None

    def run_task(self, task_id: str, *, now: datetime | None = None) -> RunOutcome:
        now = now or datetime.now(timezone.utc)
        if now.tzinfo is None:
            now = now.replace(tzinfo=timezone.utc)

        try:
            task = self._task(task_id)
        except KeyError:
            return RunOutcome(task_id, "failed", message=f"unknown task {task_id!r}")

        run_id = self._state.start_run(task_id, now=now)
        warnings: list[str] = []
        if self._output_dir is not None:
            work_dir = self._output_dir
            work_dir.mkdir(parents=True, exist_ok=True)
            own_dir = False
        else:
            work_dir = Path(tempfile.mkdtemp(prefix="p2x-"))
            own_dir = True

        try:
            articles, kept_guids = self._gather(task, now=now, warnings=warnings)
            if not articles:
                msg = self._join(warnings, "no new items")
                self._state.finish_run(run_id, status="skipped", item_count=0,
                                       message=msg, now=now)
                return RunOutcome(task_id, "skipped", 0, message=msg)

            articles = self._prepare(task, articles, warnings=warnings)
            path = self._build(task, articles, now=now, out_dir=work_dir)
            record_id = self._xteink.push_file(path, path.name)

            for feed_id, guids in kept_guids.items():
                self._state.mark_pushed(feed_id, guids, now=now)

            msg = self._join(warnings, None)
            self._state.finish_run(run_id, status="success", item_count=len(articles),
                                   file_name=path.name, message=msg, now=now)
            return RunOutcome(task_id, "success", len(articles), file_name=path.name,
                              record_id=record_id, message=msg)
        except (BuildError, XteinkUploadError) as exc:
            msg = self._join(warnings, f"{type(exc).__name__}: {exc}")
            self._state.finish_run(run_id, status="failed", message=msg, now=now)
            return RunOutcome(task_id, "failed", message=msg)
        except Exception as exc:  # noqa: BLE001 - never let a run crash the scheduler
            msg = self._join(warnings, f"unexpected error: {exc!r}")
            self._state.finish_run(run_id, status="failed", message=msg, now=now)
            return RunOutcome(task_id, "failed", message=msg)
        finally:
            if own_dir:
                shutil.rmtree(work_dir, ignore_errors=True)
```

加 `from .builders.common import BuildError` 和 `from .xteink import XteinkUploadError`。

- [ ] **Step 4: 运行完整套件 → 全绿。Commit** `feat: Pipeline.run_task orchestration with failure isolation`

---

## Task 6: CLI（`run` / `list`）

**Files:**
- Create: `src/push2xteink/cli.py`
- Create: `src/push2xteink/__main__.py`
- Create: `tests/test_cli.py`

**Interfaces:**
- Consumes: `config.load_config`, `config.ConfigError`, `state.State`, `pipeline.Pipeline`
- Produces: `cli.main(argv: list[str] | None = None) -> int`

**行为**：
- `argparse`：全局 `--config`（默认 `os.environ.get("CONFIG_PATH", "data/config.yaml")`）、`--db`（默认 `os.environ.get("DB_PATH", "data/state.db")`）。子命令 `list`、`run <task_id>`。
- `load_config` 抛 `ConfigError` → 打印到 stderr，返回 `2`。
- `list`：每个 task 一行 `id\tname\tschedule\tsummarize=..\tformat=..\tenabled=..`，返回 `0`。
- `run <task_id>`：`with Pipeline.from_config(config, state) as p: outcome = p.run_task(task_id)`；打印 `status items=.. file=.. record=..` 到 stdout；`outcome.message` 有则打印到 stderr；返回 `0`（success/skipped）或 `1`（failed）。
- `finally: state.close()`。

- [ ] **Step 1: 写失败测试**

```python
import shutil
from pathlib import Path

import pytest

from push2xteink.cli import main
from push2xteink.pipeline import RunOutcome

FIXTURE = Path(__file__).parent / "fixtures" / "config_valid.yaml"


def _cfg_file(tmp_path):
    dst = tmp_path / "config.yaml"
    shutil.copy(FIXTURE, dst)
    return dst


def test_list_prints_tasks(tmp_path, capsys):
    rc = main(["--config", str(_cfg_file(tmp_path)), "--db", str(tmp_path / "s.db"), "list"])
    assert rc == 0
    out = capsys.readouterr().out
    assert "brief" in out and "0 7 * * *" in out


def test_run_success_returns_0(tmp_path, capsys, monkeypatch):
    fake = type("P", (), {
        "__enter__": lambda self: self, "__exit__": lambda *a: None,
        "run_task": lambda self, tid, **kw: RunOutcome(tid, "success", 3, file_name="f.epub", record_id="r"),
    })
    monkeypatch.setattr("push2xteink.cli.Pipeline",
                        type("F", (), {"from_config": staticmethod(lambda *a, **k: fake())}))
    rc = main(["--config", str(_cfg_file(tmp_path)), "--db", str(tmp_path / "s.db"), "run", "brief"])
    assert rc == 0
    assert "success" in capsys.readouterr().out


def test_run_failed_returns_1(tmp_path, capsys, monkeypatch):
    fake = type("P", (), {
        "__enter__": lambda self: self, "__exit__": lambda *a: None,
        "run_task": lambda self, tid, **kw: RunOutcome(tid, "failed", message="boom"),
    })
    monkeypatch.setattr("push2xteink.cli.Pipeline",
                        type("F", (), {"from_config": staticmethod(lambda *a, **k: fake())}))
    rc = main(["--config", str(_cfg_file(tmp_path)), "--db", str(tmp_path / "s.db"), "run", "brief"])
    assert rc == 1
    assert "boom" in capsys.readouterr().err


def test_bad_config_returns_2(tmp_path, capsys):
    bad = tmp_path / "bad.yaml"
    bad.write_text("xteink: [unclosed\n", encoding="utf-8")
    rc = main(["--config", str(bad), "--db", str(tmp_path / "s.db"), "list"])
    assert rc == 2
    assert capsys.readouterr().err


def test_env_var_config_path(tmp_path, capsys, monkeypatch):
    monkeypatch.setenv("CONFIG_PATH", str(_cfg_file(tmp_path)))
    monkeypatch.setenv("DB_PATH", str(tmp_path / "s.db"))
    assert main(["list"]) == 0
```

> `tests/fixtures/config_valid.yaml` 已在 P1 存在。若它的 task id 不是 `brief` / schedule 不是 `0 7 * * *`，改断言以匹配该文件的真实内容（先 `cat` 一下）。

- [ ] **Step 2: 运行 → 失败**

- [ ] **Step 3: 实现**

`src/push2xteink/cli.py`：

```python
from __future__ import annotations

import argparse
import os
import sys
from pathlib import Path

from .config import ConfigError, load_config
from .pipeline import Pipeline
from .state import State


def _parser() -> argparse.ArgumentParser:
    p = argparse.ArgumentParser(prog="push2xteink")
    p.add_argument("--config", default=os.environ.get("CONFIG_PATH", "data/config.yaml"))
    p.add_argument("--db", default=os.environ.get("DB_PATH", "data/state.db"))
    sub = p.add_subparsers(dest="cmd", required=True)
    sub.add_parser("list", help="list configured tasks")
    run = sub.add_parser("run", help="run one task now")
    run.add_argument("task_id")
    return p


def main(argv: list[str] | None = None) -> int:
    args = _parser().parse_args(sys.argv[1:] if argv is None else argv)

    try:
        config = load_config(Path(args.config))
    except ConfigError as exc:
        print(f"config error: {exc}", file=sys.stderr)
        return 2

    state = State(Path(args.db))
    try:
        if args.cmd == "list":
            for t in config.tasks:
                print(
                    f"{t.id}\t{t.name}\t{t.schedule}\t"
                    f"summarize={t.summarize}\tformat={t.format}\tenabled={t.enabled}"
                )
            return 0

        # args.cmd == "run"
        with Pipeline.from_config(config, state) as pipe:
            outcome = pipe.run_task(args.task_id)
        print(
            f"{outcome.status} items={outcome.item_count} "
            f"file={outcome.file_name} record={outcome.record_id}"
        )
        if outcome.message:
            print(outcome.message, file=sys.stderr)
        return 0 if outcome.status in ("success", "skipped") else 1
    finally:
        state.close()
```

`src/push2xteink/__main__.py`：

```python
import sys

from .cli import main

sys.exit(main())
```

- [ ] **Step 4: 运行完整套件 → 全绿。手动冒烟**：`python -m push2xteink --help` 和 `python -m push2xteink --config tests/fixtures/config_valid.yaml --db /tmp/x.db list` 应正常输出。**Commit** `feat: CLI (run / list) + __main__`

---

## Self-Review

**Spec coverage（第 6 节 0→7）：**
- 步骤 0（runs 插 running）→ Task 5 `state.start_run`。
- 步骤 1（拉源，proxy/timeout，失败记 warning 不抛）→ Task 2 `_gather`。
- 步骤 2（去重，first_run 窗口，写 seen_items）→ Task 2 委托 `select_new_articles`（P2a）；`first_run = not task_has_successful_run`。
- 步骤 3（full_text 并发，回退）→ Task 3 `_prepare` + `ThreadPoolExecutor`；回退逻辑在 `apply_full_text` 内（P2a）。
- 步骤 4（summarize，失败跳过记 warning）→ Task 3 `_prepare`；摘要输入过 `html_to_text`。
- 步骤 5（标题 `{name}_{YYYYMMDD}`，同日重跑加 `_{HHMMSS}`，epub/txt，<256B 抛）→ Task 4 `_build`。
- 步骤 6（push_file；成功才 mark_pushed；失败 status=failed 不 mark）→ Task 5。
- 步骤 7（更新 runs 行）→ Task 5 `finish_run`。
- 「去重与重试一致性」→ 上传失败不 mark_pushed，`select_new_articles` 已 `record_seen(pushed_at=NULL)`，下次窗口内重试（`test_run_task_upload_failure_does_not_mark_pushed` 锁定）。
- 失败隔离四条（单源/单条正文/单条摘要/上传）→ Task 2、`apply_full_text`、Task 3、Task 5 分别覆盖。
- 第 14 节：`Pipeline` 持长期 `Summarizer`/`XteinkClient`，`close()` + ctx mgr；摘要输入用 `html_to_text`。
- 第 10 节：CLI 读 `CONFIG_PATH` / `DB_PATH`。

**Placeholder scan:** Task 1 的 `run_task` 桩 `raise NotImplementedError` 标注 Task 5，Task 5 给出完整实现。无 TODO / 占位。

**Type consistency:** `RunOutcome` 字段、`Pipeline.__init__` / `from_config` / `run_task` / `_gather` / `_prepare` / `_build` 签名在 Interfaces / 各 Task Produces / 实现 / 测试一致。所有 P1/P2 调用点对照「依赖签名」块逐一核对（`fetch_feed` kwargs、`select_new_articles` kwargs、`apply_full_text` kwargs、`build_epub/txt` kwargs、`Summarizer(config.ai, proxy_url=...)`、`XteinkClient(config.xteink, state)`、`State.finish_run` kwargs）。`cli.main` 返回 `int`，`__main__` `sys.exit(main())`。

**并发/线程安全:** `ThreadPoolExecutor` 每次 `_prepare` 新建、用完即弃。`Pipeline.run_task` 对同一 task 由 P4 的 `max_instances=1` 保证不并发；不同 task 并发时共享 `state`（P1 线程安全）、`summarizer`（内部锁）、`xteink`（经线程安全 state）——安全。`tempfile.mkdtemp` 每 run 唯一，`finally` 清理。

**已知延后（不在本计划）：** `ProxyConfig.url` scheme 校验（P4/P5）、`Article.published_at` AwareDatetime（P1 follow-up）、`Summarizer` 退避重试——见 spec 第 14 节。
